# Tested on Python 3.13. Versions below are what this project was built with;
# >= constraints are given for portability.
pandas>=2.2          # built with 3.0.3
numpy>=1.26          # built with 2.4.6
matplotlib>=3.8      # built with 3.10.9
duckdb>=1.0          # built with 1.5.3
python-docx>=1.1     # built with 1.2.0 (report generation)
pymupdf>=1.24        # optional: local PDF render for visual QA of the .docx
